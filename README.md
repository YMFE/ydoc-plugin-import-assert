# ydoc-plugin-import-assert
import js and css

## Install
```
npm install ydoc-plugin-import-assert
```
## Usage

In the ydoc config file, configuration:

```
{
  plugins: [{
    name: 'import-assert',
    options: {
      css: 'custom.css',
      js:  'custom.js'
    }
  }]
}
```
